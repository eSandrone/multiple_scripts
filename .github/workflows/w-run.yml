name: w-run
on:
  workflow_run:
   workflows: ["Time"]
   types:
    - completed
jobs:
  job_2:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup python
        uses: actions/setup-python@v4
        with:
          python-version: "pypy3.9"
        
      - name: Try
        run: |
          echo ${{ github.event.workflow_run.id }}

      - name: Unzip and read
        run: |
          pip install requests
          python ./python/get-logs.py ${{ github.event.workflow_run.id }}
          ls
          #echo ${{ github.run_id }}
          #url="https://api.github.com/repos/eSandrone/multiple_scripts/actions/runs/${{ github.event.workflow_run.id }}/logs"
          #echo $url
          #curl -L eSandrone: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 -H "Accept: application/vnd.github+json" --url "$url"
          #ls
          #unzip *.zip
          #ls
